
# CheckPoint AI 〜AIによる業務報告の要約と分析ツール〜

## アプリの概要

このアプリを一言でいうと、 **「AIがマネージャーの代わりをしてくれる、お仕事報告管理ツール」** です。
単に業務報告を保存するだけでなく、Python（FastAPI）とGemini AIを連携させ、投稿内容から「仕事の危険度」を自動でスコア化し、要約まで行うことで、チームの状況をひと目で把握できる仕組みを実現しました。

https://github.com/user-attachments/assets/810ce9bd-8881-402e-9301-9ff4f4d25aa1


https://github.com/user-attachments/assets/145c4089-8f60-42f2-87df-7789fdb7ee9b

---

### 開発の背景

「あれ、あの件どうなったっけ？」「さっき言ったよね？」といった、職場での **「言った・言わない」のトラブルをなくしたい**と思ったのが開発のきっかけです。

また、忙しい時に口頭で報告されると、作業を一度止めなければならず、お互いに集中力が切れてしまうことがあります。このアプリなら、 **「報告する側は好きな時に送れる」「確認する側は手が空いた時にチェックできる」** という、自分のペースで仕事ができる環境を作れます。

さらに、溜まった報告を一つずつ読むのは大変ですが、**AIがパッと要約してくれたり、仕事の調子をグラフにしてくれる**ことで、「今はどこに力を入れるべきか」がすぐに分かり、チーム全体の仕事がもっとスムーズに進むようになることを目指しました。

これまで学んできたPHP/Laravelの技術に加え、フロントエンドに**Next.js (App Router)**、AI解析に**Python**を導入。「人間が全部読まなくても、AIがリスクをあぶり出してくれる親切な設計」を目指して開発しました。

---

### こだわったポイント


**1. 「言った・言わない」をなくす確実な記録**
*   口頭だと忘れてしまいがちな報告を、文字と時間でしっかり残します。
*   後からいつでも見返せるので、「あの時どう決まったっけ？」という確認作業がなくなります。

**2. 集中力を切らさない「非同期」のコミュニケーション**
*   口頭報告のように相手の手を止める必要がありません。
*   コメント機能を使って、お互い好きなタイミングで内容の確認やアドバイスができるので、自分の作業に集中できる時間を増やせます。

**3. AIによる「仕事の健康診断」**
*   溜まったデータをAIが分析して、「今、仕事が捗っているか」「改善すべき点はあるか」をまとめてくれます。
*   自分で計算しなくても、AIが客観的な数字で教えてくれるので、次の仕事の作戦が立てやすくなります。

**4. 「一瞬で状況がわかる」リスク検知ロジック**
* 投稿された文章をPythonが解析し、「順調」「要確認」「リスクあり」の3段階で自動仕分けします。
* 深刻なトラブルを抱えた報告には赤いアラートを表示し、管理者が**「今、誰を助けるべきか」を1秒で判断できる**ように工夫しました。

**5. 「読む時間を9割削る」AI要約機能**
* 長い報告文も、AIが重要なポイントを3行程度に要約します。
* 忙しい朝の進捗確認でも、要約をパッと眺めるだけでチーム全体の動きを把握できる「タイパ（タイムパフォーマンス）」にこだわりました。

**6. 「直感的に推移を掴む」分析グラフ**
* 毎日の感情・進捗スコアを蓄積し、Recharts を使ってグラフ化しました。
* 「先週から急にスコアが下がっている」といった目に見えないチームの予兆を、客観的な数字でキャッチできるようにしています。

**7.「止まらない」快適な操作感**
* Next.jsの特性を活かし、画面全体を読み込み直さずに投稿やコメントができるスムーズな動きを追求しました。
* AIの解析待ちの間もスケルトンスクリーンを出すなど、ユーザーを待たせないUI設計を心がけています。
* 分析データのAI詳細要約は時間がかかるので下部におき、上部のデータは比較的早く表示されるようにuseEffectを分けて設定しました。
* AI詳細分析は３０分後にリロードした場合再取得設定。

**8. 「見逃さない」リアルタイム通知機能**
* 自分の報告書にコメントがついた瞬間、ヘッダーのベルアイコンに未読バッジが表示されます。
* 30秒ごとに自動で未読件数を取得するポーリング設計で、ページを再読み込みしなくても通知に気づけます。
* 通知をクリックすると該当の報告書に自動スクロール＆ハイライト表示され、「どの報告へのコメントか」を瞬時に確認できます。
* 自分の報告書に自分でコメントした場合は通知を生成しない制御を実装し、不要な通知を排除しています。
* Laravelのスケジューラーを使い、7日以上経過した通知を毎日深夜0時に自動削除。DBに不要なデータが溜まり続けない設計にしました。


**9. 「自己解決を促す」AIナレッジ検索（RAG）**
* 社内マニュアル（PDF等）をAIが事前学習なしに読み取り、質問に回答する機能を実装しました。
* 「有給の申請は何日前まで？」といった、わざわざ人に聞くほどではないけれど確認が必要な事項をAIが即答。管理者の回答コストを削減します。



---

## ■ 苦労した点と解決したアプローチ

### 1. 異なる言語・技術の連携（Next.js × Laravel × Python）

* **課題**
Laravelを全体の「司令塔」として、画面（Next.js）からのリクエストを受け取り、Pythonへ解析を依頼し、その結果をデータベースへ保存するという、複数の技術をまたぐデータ処理の流れを制御するロジックの構築に苦労しました。
* **解決策**
LaravelからPythonの実行コマンドを動かす処理を実装し、受け渡すデータを共通の形式（JSON）に統一しました。データのバトンタッチをシンプルにしたことで処理が途中で止まるのを防ぎ、安定した連携ロジックを構築できました。

### 2. Dockerによる多層環境の構築

* **課題**
画面、サーバー、データベースに加え、解析用のPython環境まで、性質の違う複数のコンテナを1つのDockerネットワーク内で共存・通信させる設定に非常に苦労しました。
* **解決策**
Docker内のネットワーク構造を学び、それぞれのコンテナがどのポート（通り道）を使ってデータをやり取りするべきかを1つずつ整理・指定しました。全環境がスムーズに連携して動くインフラを自力で構築できたことで、環境構築への理解が大きく深まりました。

### 3. 保守性を意識した「型定義（TypeScript）」の徹底

* **課題**
これまでは画面側に直接データを書いてしまうこともありましたが、今回は実務を意識し、データのやり取りに厳密な「型（データのルール）」を定義することに挑戦しました。最初は型エラーに苦戦しました。
* **解決策**
プログラムが受け取るデータ（投稿、コメント、ユーザー情報など）の形をあらかじめ厳しく決めておくことで、流れてくるデータの中身が明確になりました。結果として、データのすれ違いによるバグを未然に防ぎ、安全に開発を進めることができました。

### 4. ロジックの共通化（カスタムフックの活用）

* **課題**
「データの取得」や「投稿・削除処理」などの複雑な仕組みをそのまま画面のコード（コンポーネント）の中に書くと、コードが長くなり、どこで何をしているか分かりづらくなる課題がありました。
* **解決策**
見た目を作るコードと、データを処理するロジックのコードを完全に切り離し、処理専用の部品（カスタムフック）としてまとめました。これにより、画面側のコードがスッキリし、修正や機能追加がしやすい「読みやすいコード」を実現しました。

### 5. Laravel・Next.js間の認証（CSRF）の壁

* **課題**
通知を既読にする処理（PUTリクエスト）の際、セキュリティ上のエラー（419エラー）が発生して処理が止まってしまいました。
* **解決策**
原因を調べたところ、他の処理では通信時にセキュリティ用の合言葉（CSRFトークン）を自動で付けてくれるツール（axios）を使っていたのに対し、通知の処理だけ別の仕組み（fetch）を使っていたため、合言葉が送られていないことが分かりました。プロジェクト全体で通信ツールを「axios」に統一することで、エラーを解決しました。

### 6. 複数コンテナ間のデバッグ

* **課題**
画面、サーバー、Pythonの3つの層に分かれているため、バグが起きたときに「どの場所でエラーが起きているか」を特定するのが難しかったです。
* **解決策**
ブラウザの通信記録（Networkタブ）、Laravelのログファイル、Pythonのログの3つを組み合わせて、原因を1つずつ切り分ける力を身につけました。これにより、複雑なシステムでも冷静に問題の場所を見つけられるようになりました。

### 7. ルートモデルバインディングの命名ルール

* **課題**
URLに含まれるパラメーターと、Laravelのプログラム（Controller）側で受け取る変数名が一致していないと、データベースから自動でデータを取得してくれないという仕様に気づかず、処理が動かずにハマりました。
* **解決策**
名前の不一致が原因だと突き止め、命名ルールを完全に一致させることで解決しました。プログラムが自動で動いてくれる仕組みの裏側にある「ルールの重要性」を実感しました。

### 8. 追加のAI機能：外部サービス依存からの脱却（安定性の向上）

* **課題**
説明書（マニュアル）から必要な情報を探して回答するAI機能を開発する際、当初は言葉の分析をすべて外部のクラウドサービス（Google API）に依頼していました。しかし、通信トラブルなどにより「ページが見つかりません（404エラー）」が発生し、システム全体が止まってしまう課題に直面しました。
* **解決策**
エラーの原因が外部との通信にあると特定し、クラウドに頼るのをやめて、自分のサーバー（コンテナ）内で完結する仕組み（ローカル完備型モデル）へ切り替えました。
* **下準備（検索）**：自分のコンテナ内にある「AI辞書」を使い、マニュアルから必要な箇所を特定。
* **仕上げ（回答）**：切り出した情報だけを外部APIに渡し、自然な日本語に清書。


* **結果**
この役割分担の設計に変えたことで、外部サービスの通信トラブルに左右されなくなりました。どんな時でも安定して正しい回答を生成できる、信頼性の高いAIの仕組みを構築することができました。

---



## システム連携図（AI分析の流れ）

### 報告投稿時の「ロジック」

| ステップ | 担当 | 役割 |
| --- | --- | --- |
| **1. 報告する** | Next.js | ユーザーが業務内容を入力 |
| **2. 依頼を投げる** | Laravel | データを保存し、Python(FastAPI)へ解析依頼 |
| **3. AI解析** | Python (Gemini) | 内容をスコア化（0〜100点）し、要約文を作成 |
| **4. 可視化** | Next.js | スコアに基づき、バッジの表示やグラフを即座に更新 |



---

## 主な機能

1. **AI自動スコアリング＆要約**
投稿した瞬間にAIが内容を分析。ポジティブ・ネガティブ度を数値化し、要約文を生成します。
2. **ダッシュボード（分析表示）**
チーム全体の平均スコアや、リスク報告の件数をカード形式で表示。グラフにより長期的な傾向を分析できます。
3. **ステータスバッジ機能**
AIが判定した状況（順調・要確認・リスクあり）に応じて、一覧画面にカラーバッジを表示。視覚的な優先順位付けを助けます。
4. **コメント＆コミュニケーション**
各報告に対して、その場でフィードバックや質問を送れます。口頭報告とは違い、**「報告者は好きな時に送り、確認者は手が空いた時に返信できる」**ため、お互いの作業の手を止めることなく、集中力を維持したままスムーズに内容確認が行えます。
5. **AI詳細分析（インサイト）**
蓄積されたデータから、AIが「来週の予測」や「現在のトピック（頻出ワード）」を抽出してアドバイスを提示します。
6. **リアルタイム通知機能**
自分の報告書にコメントがつくと、ヘッダーのベルアイコンに未読数バッジが表示されます。通知をクリックすると該当報告書へ自動スクロール＆ハイライトされ、既読管理も自動で行われます。7日経過すると自動消去される設定をしています。
7. **プロフィール管理**
マイページから名前の変更と、自分が投稿した報告書の一覧確認ができます。
8. **ページネーション**
報告書が増えても快適に閲覧できるよう、一覧表示にページネーションを実装しています。
9.  **AI相談室（マニュアル検索機能）**
    `./data` フォルダに配置した業務マニュアル（PDFやテキスト）の内容をAIが理解し、チャット形式で回答します。最新の「LlamaIndex」ライブラリを活用し、大規模なマニュアルから必要な箇所だけを瞬時に見つけ出す仕組み（RAG）を構築しました。

機能要件：https://docs.google.com/spreadsheets/d/1DTRqEVQvEwA02As2Awfg9wv-ecgeEGetrDaqXLCTJsw/edit?usp=sharing

---

## 使用技術

- Next.js 15+ (App Router)
- TypeScript
- Tailwind CSS
- React Hooks (useState, useEffect)
- Laravel 12
- PHP 8.4
- Laravel Sanctum
- REST API (JSON)
- MySQL
- Docker
- nginx 1.21.1
- GitHub
- npm / composer
- Axios
- Python 3.13.5
- FastAPI
- Google Gemini API
- ChatGPT
- Claude
- Gemini
- LlamaIndex（RAG構築フレームワーク）
- HuggingFace Embedding (BAAI/bge-small-en-v1.5)
- PyPDF（PDF解析）

---

## データベース設計

### 1. `users` テーブル
ユーザーの基本情報を管理します。
| カラム名 | 型 | 詳細 |
| :--- | :--- | :--- |
| **id** | BigInt | プライマリキー |
| **name** | string | 氏名 |
| **email** | string | メールアドレス (Unique) |
| **password** | string | パスワード (Hash) |
| **created_at** | timestamp | 作成日時 |
| **updated_at** | timestamp | 更新日時 |

### 2. `reports` テーブル
ユーザーが投稿した業務報告を管理します。
| カラム名 | 型 | 詳細 | 制約・リレーション |
| :--- | :--- | :--- | :--- |
| **id** | BigInt | プライマリキー | |
| **user_id** | BigInt | 投稿者ID | `users(id)` 外部キー |
| **content** | text | 報告本文 | |
| **status** | enum | 判定結果 | '順調', 'リスクあり', '要確認' |
| **score_total** | integer | AI総合スコア | |
| **score_positive** | integer | ポジティブ度 | |
| **score_negative** | integer | ネガティブ度 | |
| **ai_summary** | text | AIによる要約文 | |
| **created_at** | timestamp | 作成日時 | |
| **updated_at** | timestamp | 更新日時 | |

### 3. `comments` テーブル
報告に対する補足やフィードバックを管理します。
| カラム名 | 型 | 詳細 | 制約・リレーション |
| :--- | :--- | :--- | :--- |
| **id** | BigInt | プライマリキー | |
| **report_id** | BigInt | 対象の報告ID | `reports(id)` 外部キー |
| **user_id** | BigInt | コメント投稿者ID | `users(id)` 外部キー |
| **comment_body** | text | コメント本文 | |
| **created_at** | timestamp | 作成日時 | |
| **updated_at** | timestamp | 更新日時 | |


### 4. `notifications` テーブル
コメント通知を管理します。
| カラム名 | 型 | 詳細 | 制約・リレーション |
| :--- | :--- | :--- | :--- |
| **id** | BigInt | プライマリキー | |
| **user_id** | BigInt | 通知を受け取るユーザーID | `users(id)` 外部キー |
| **report_id** | BigInt | コメントがついた報告書ID | `reports(id)` 外部キー |
| **comment_id** | BigInt | 対象のコメントID | `comments(id)` 外部キー |
| **is_read** | boolean | 既読フラグ | デフォルト: false |
| **created_at** | timestamp | 作成日時 | |
| **updated_at** | timestamp | 更新日時 | |
---


## セットアップ方法

1. **リポジトリをクローン**
```bash
git clone git@github.com:ando625/checkAI.git
cd checkAI
```

2. **Dockerコンテナの起動**
```bash
docker compose up -d --build

```

3. snsappからフロントエンドnextで実行
```
snsapp
├──  laravel(backend)
└──  next(frontend) ← ココ
```

```
npm install
```




4. プロジェクトのルートphp上で実行
```
docker compose exec php bash
```



5. **環境設定ファイルの準備**
```bash
cp .env.example .env

```

※ .env ファイルを開き、データベースの設定を以下のように書き換えてください：

```
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=laravel_db
DB_USERNAME=laravel_user
DB_PASSWORD=laravel_pass
```

GeminiのAPIキーを貼り付ける
```
GEMINI_API_KEY=

AI_SERVICE_URL=http://python:8000
```


6. **ライブラリのインストール**

```bash
composer install
```



### サンクタムを有効にする手順

すべて `docker compose exec php bash` の中で実行します。

#### 1. API機能をインストール（Laravel 11以降の場合）
最近のLaravelは、初期状態ではAPIの口が閉じています。これでサンクタムの設定を一気に流し込みます。
```bash
php artisan install:api
```
※ これを実行すると、`config/sanctum.php` が作成され、データベースに専用のテーブルが追加され、`bootstrap/app.php` に設定が書き込まれます。

#### 2. プロバイダーの公開
設定ファイルを自分でいじれるように外に出します。
```bash
php artisan vendor:publish --provider="Laravel\Sanctum\SanctumServiceProvider"
```

#### 3. マイグレーションの実行
サンクタムが使うテーブル（トークン管理用）を作成します。
```bash
php artisan migrate
```




7. **アプリケーションキーの生成とデータベース作成**
```bash
php artisan key:generate
php artisan migrate:fresh --seed
```



### `.env.local` を作成する

Next.jsのディレクトリ（`next` フォルダの直下）に `.env.local` という名前のファイルを手動で作成し、以下の内容を貼り付けてください。

```env
# Laravel（バックエンド）のURL
NEXT_PUBLIC_BACKEND_URL=http://localhost

```

### Python側に `.env` を作成する手順

1. Pythonのディレクトリに移動する
   Macのターミナルで、プロジェクトの `python` フォルダ（`main.py` などがある場所）に移動します。

2. `.env` ファイルを新規作成する
   ```bash
   touch .env
   ```

3. 中身を書き込む
   作成した `.env` をテキストエディタ（VSCodeなど）で開き、以下の内容を書き込んで保存してください。

   ```env
   GEMINI_API_KEY=APIキーをここに貼り付け
   ```

4. コンテナを再起動して設定を反映させる
   設定を読み込ませるために、Pythonコンテナを一度再起動します。
   ```
   docker compose restart checkai-python-1
   ```

1. プロジェクトルートに移動（もしいなければ）
```
cd ~/checkAI
```

2. 新しいキーで上書き
```
echo "GEMINI_API_KEY=API作成キー" > .env
```

3. 確認
```
cat .env
```
#### GEMINI_API_KEY=取得したAPI作成キー と表示されればOK

4. Dockerを完全再起動
```
docker compose down
docker compose up -d
```

5. キーが届いているか確認
```
docker compose exec python env | grep GEMINI
```
#### GEMINI_API_KEY=API作成キー と表示されればOK




---

### アクセス情報
* **Frontend**:  http://localhost:3000 
* **Backend API**:  http://localhost:80 

---

#### テストユーザー

| メールアドレス        | パスワード |
|-----------------------|------------|
| yamada@example.com | pass1234   |

| メールアドレス        | パスワード |
|-----------------------|------------|
| yoshieda@example.com | pass1234   |

| メールアドレス        | パスワード |
|-----------------------|------------|
| ootani@example.com | pass1234   |

| メールアドレス        | パスワード |
|-----------------------|------------|
| suzuki@example.com | pass1234   |



**UserSeeder.phpで作成しているのでログインする前に一度確認してください**

ReportSeederに過去３ヶ月分のダミーデータを作成済み



---


## phpMyAdmin

- URL: http://localhost:8080/
- ユーザー名・パスワードは `.env` と同じ
- DB: `laravel_db` を確認可能






